<odoo>
  <data>
    <template id="order_download_page" name="Descargas de fotos">
      <t t-call="website.layout">
        <div id="wrap" class="oe_structure">
          <section class="py-5 bg-200">
            <div class="container">
              <nav aria-label="breadcrumb" class="mb-3">
                <ol class="breadcrumb">
                  <li class="breadcrumb-item"><a href="/shop/confirmation">Confirmación</a></li>
                  <li class="breadcrumb-item active" aria-current="page">Descargas</li>
                </ol>
              </nav>
              <div class="card shadow-sm">
                <div class="card-body">
                  <h1 class="h4 mb-3">Descargá tus fotos sin marca de agua</h1>
                  <t t-if="downloads">
                    <p class="text-muted">Cada archivo corresponde a la compra de la orden <strong t-esc="order.name"/>.</p>
                    <div class="list-group">
                      <a t-foreach="downloads" t-as="item" t-att-href="item['url']" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        <div>
                          <strong t-esc="item['asset'].name or ('Foto %s' % item['asset'].id)"/>
                          <p class="mb-0 text-muted small">
                            Evento: <t t-esc="item['asset'].evento_id.name"/>
                          </p>
                        </div>
                        <span class="btn btn-sm btn-outline-primary">Descargar</span>
                      </a>
                    </div>
                  </t>
                  <t t-else="">
                    <p class="text-muted mb-0">Aún no hay descargas disponibles para esta orden.</p>
                  </t>
                </div>
              </div>
              <div class="mt-3">
                <a class="btn btn-secondary" t-att-href="'/fotoapp/orders/%s/summary' % order.id">Volver al resumen</a>
              </div>
            </div>
          </section>
        </div>
      </t>
    </template>

    <template id="confirmation_downloads_cta" inherit_id="website_sale.confirmation" name="Fotoapp Download CTA">
      <xpath expr="." position="inside">
        <t t-set="foto_lines" t-value="order.order_line.filtered(lambda line: line.foto_asset_id)"/>
        <t t-if="foto_lines">
          <section class="py-4">
            <div class="container">
              <div class="card shadow-sm border-0">
                <div class="card-body d-flex flex-column flex-lg-row align-items-lg-center justify-content-between gap-3">
                  <div>
                    <h3 class="h5 mb-1">Descargá tus fotos originales</h3>
                    <p class="text-muted mb-0">Accedé a los archivos sin marca de agua apenas confirmás la compra.</p>
                  </div>
                  <a class="btn btn-primary" t-att-href="'/fotoapp/orders/%s/downloads' % order.id">Descargar fotos</a>
                </div>
              </div>
            </div>
          </section>
        </t>
      </xpath>
    </template>
  </data>
</odoo>
